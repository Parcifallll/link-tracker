server:
  port: 8080

spring:
  application:
    name: bot
  output:
    ansi:
      enabled: always

app:
  telegram:
    url: https://api.telegram.org/bot
    token: ${APP_TELEGRAM_TOKEN}
    update-listener-sleep: 1s
    debug: false

logging:
  structured:
    format:
      console: ecs
